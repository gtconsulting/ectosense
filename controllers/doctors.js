const User = require("../models/User");
const Clinic = require("../models/Clinic");
const Appointment = require("../models/Appointment");
const UserController = require("./users");

module.exports = {

    getAppointments: async (reqObj, resObj) => {
        // let appointments = 
    }
}